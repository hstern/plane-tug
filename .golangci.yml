# golangci-lint v2 schema.
version: "2"

run:
  concurrency: 4
  timeout: 5m
  tests: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 50

linters:
  enable:
    # correctness
    - govet
    - staticcheck
    - unused
    - errcheck
    - errorlint
    - ineffassign
    # style
    - revive
    - gocritic
    - misspell
    - nolintlint
    # security & resources
    - gosec
    - bodyclose
    - noctx
  settings:
    errcheck:
      check-type-assertions: true
    gosec:
      excludes:
        # G304: file inclusion via variable. We deliberately read user-provided
        # config paths; the path is the operator's input.
        - G304
        # G101: hardcoded credentials. False-positive on identifiers like
        # envWebhookSecret whose value is the *name* of an environment
        # variable, not a credential.
        - G101
    nolintlint:
      require-explanation: true
      require-specific: true
  exclusions:
    rules:
      # Tests get a bit more rope: unchecked errors, gosec false positives
      # (G404 random, hardcoded creds in fixtures), and noctx complaints
      # about httptest.NewRequest / client.Get — none of which exercise real
      # network paths.
      - path: _test\.go
        linters:
          - errcheck
          - gosec
          - noctx

formatters:
  enable:
    - gofmt
    - goimports
